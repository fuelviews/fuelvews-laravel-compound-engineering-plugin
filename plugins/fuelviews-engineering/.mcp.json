{
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "x-api-key": "${CONTEXT7_API_KEY:-}"
      }
    },
    "gitnexus": {
      "command": "npx",
      "args": ["-y", "gitnexus@1", "mcp"],
      "disabled": true,
      "_comment": "Enable after running: npm install -g gitnexus@1 && gitnexus analyze. The fv:start-session skill will offer to install and enable this."
    }
  }
}
